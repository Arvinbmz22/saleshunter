# AdminExt AI Lead Finder — مستندات پروژه

موتور کشف لید B2B از **منابع عمومی وب** برای ShopBot.  
اصل: **کیفیت مهم‌تر از تعداد است.** این برنامه بات اینستاگرام، ورود به اکانت، یا ارسال پیام خودکار نیست.

> ظاهر شدن در نتایج جستجوی مرتبط با ایران، هویت ایرانی بودن کسب‌وکار را ثابت نمی‌کند.  
> احراز ایران نیاز به چند سیگنال عمومی معنادار و **مستقل** دارد.

---

## ۱. چه ساخته شده است

پوشه `D:\SHOPBOT\Sales-Hunt\` در شروع خالی بود. از صفر یک اپ Next.js (App Router + TypeScript) پیاده شد و روی **http://localhost:3500** بالا آمد.

کارهای انجام‌شده تا این نسخه:

1. اسکلت پروژه، مدل لید، ذخیره JSON، انتزاع SearchProvider / AIProvider  
2. پایپلاین چندمرحله‌ای جستجو → استخراج → فیلتر → گیت ایران → کسب‌وکار → فروش آنلاین → امتیاز → رتبه  
3. داشبورد فارسی RTL با SSE برای پیشرفت واقعی مراحل  
4. تست‌های واحد (Vitest) برای ایران، کسب‌وکار، فروشگاه، رتبه، دِدوپ، AI، کوئری، Provider  
5. فونت Vazirmatn  
6. جستجوی پیشرفته‌تر: مترادف صنف، کوئری موازی، حذف URL تکراری، جستجوی تطبیقی، فیلتر ارزان  
7. صفحه **لیدهای قبلی** برای لیدهایی که ذخیره شده‌اند و در جستجوی جدید به‌خاطر تکراری بودن دیگر نشان داده نمی‌شوند  

دیپلوی سرور هنوز انجام نشده (طبق درخواست، بعد از تأیید لوکال).

---

## ۲. معماری

```
UI فارسی RTL  (پورت 3500)
  ├── POST /api/search   SSE  → پایپلاین کشف لید جدید
  └── GET  /api/leads         → آرشیو لیدهای ذخیره‌شده

پایپلاین:
  تولید کوئری (چند خانواده + مترادف)
    → SearchProvider.search()  موازی، با بودجه
    → حذف URL تکراری
    → استخراج کاندیدا + ادغام هویت
    → تشخیص الگوی شکست → کوئری تطبیقی دوباره
    → دِدوپ تاریخی/جلسه‌ای
    → فیلتر ارزان (خبر / ویکی / ویدیو)
    → گیت احراز ایران
    → احراز کسب‌وکار
    → احراز فروش آنلاین
    → فعالیت، تماس، ShopBot Fit
    → AI اختیاری (JSON با اسکیما)
    → امتیاز + رتبه
    → فقط لید NEW در پاسخ + append به data/leads.json
```

کلید API فقط سمت سرور است و به فرانت نمی‌رود.

---

## ۳. درخت فایل

```
app/
  layout.tsx              فونت Vazirmatn، html dir=rtl lang=fa
  page.tsx                داشبورد جستجو
  globals.css
  leads/page.tsx          آرشیو لیدهای قبلی
  api/search/route.ts     SSE جستجو
  api/leads/route.ts      GET لیدهای ذخیره‌شده
components/
  SearchForm.tsx
  SearchStatus.tsx
  LeadTable.tsx
  LeadCard.tsx
lib/
  leads/                  storage, normalize, duplicate
  search/                 provider, queries, synonyms, discovery, execute
  verification/           iran, business, shop, activity, evidence
  scoring/                score, ranking
  ai/provider.ts
  pipeline/               run.ts, labels.ts
  security/               validate.ts, ssrf.ts
types/lead.ts
data/leads.json
tests/
PROJECT_DOCS.md
.env.example
.env.local                (gitignored) فعلاً SEARCH_PROVIDER=mock
```

---

## ۴. مدل داده لید

ذخیره در `data/leads.json`. رکوردهای قدیمی با فیلد کم، در `loadLeads()` مقدار پیش‌فرض می‌گیرند.

فیلدهای اصلی: `username`, `instagramUrl`, `businessName`, `category`, `city`, `country`, `bio`, `website`, `telegram`, `whatsapp`, `followers`, `businessType`, `evidence`, `sourceUrls`, امتیاز و `scoreBreakdown`, `tier`, `reason`, `provider`, `discoveredAt`.

احراز ایران: `iranVerificationStatus`, `iranVerificationScore`, `iranConfidence`, `iranEvidence`.

سایر: وضعیت کسب‌وکار/فروشگاه/فعالیت، پتانسیل پیام مشتری، ShopBot fit، اعتبار، کیفیت شواهد، هشدار، دلیل رد.

اطلاعات ناموجود = `null`. هیچ فکت ساختگی ساخته نمی‌شود.

---

## ۵. رابط کاربری

- صفحه اصلی: عنوان AdminExt AI Lead Finder، فرم صنف / کشور / شهر / تعداد لید جدید  
- دکمه‌ها: **شروع جستجو** و **لیدهای قبلی**  
- حین جستجو: برچسب مراحل واقعی پایپلاین (نه پیشرفت جعلی)  
- بعد از اتمام: تعداد لید جدید، تکراری حذف‌شده، نامرتبط، رد عدم احراز ایران  
- جدول: ردیف، نام، اینستاگرام قابل کلیک، شهر، دسته، امتیاز، سطح، دلیل، لینک  
- کارت جزئیات: شواهد ایران، شکست امتیاز، هشدار  
- حالت MOCK با بنر زرد مشخص می‌شود  
- فونت: Google Font **Vazirmatn** (۴۰۰–۷۰۰) از `next/font`  
- آرشیو `/leads`: همه لیدهای JSON به‌ترتیب جدیدترین؛ توضیح که این‌ها در جستجوی بعدی تکراری محسوب می‌شوند  

---

## ۶. استراتژی جستجو

`SearchProvider.search(query)` — به یک موتور قفل نشده.

| مقدار | رفتار |
| --- | --- |
| `SEARCH_PROVIDER=mock` | داده نمونه برچسب‌خورده |
| `tavily` / `serper` / `brave` | جستجوی واقعی با کلید env |
| هیچ‌کدام و بدون کلید | **خطای شفاف** — نتیجه جعلی ساخته نمی‌شود |

Mock به‌عنوان fallback خاموش برای Provider واقعی استفاده نمی‌شود. Fixtureهای mock فقط برای صنف منطبق برمی‌گردند (آرایشی جدا از پوشاک و غیره).

### خانواده‌های کوئری

- صنف + کشور/شهر  
- `site:instagram.com` با واژه‌های فروش/خرید/فروشگاه  
- زبان محلی و نیت تجاری (دایرکت، واتساپ، تومان، سفارش، …)  
- سایت `.ir` و «سایت رسمی»  
- مترادف صنف (`lib/search/synonyms.ts`) مثلاً لوازم آرایشی → cosmetics / makeup  

سقف حدود ۴۸ کوئری یکتا. بودجه با `SEARCH_BUDGET` (پیش‌فرض لوکال ۴۸، سقف ۱۲0).

### اجرای جستجو (`lib/search/execute.ts`)

- موازی (mock: ۶، واقعی: ۳)  
- حذف URL تکراری  
- اگر همه درخواست‌ها fail شوند، خطا برمی‌گردد نه نتیجه ساختگی  

### جستجوی تطبیقی

بعد از دسته اول، الگوی شکست تشخیص داده می‌شود:

- زیاد اینفلوئنسر → کوئری فروشگاه/ثبت سفارش/واتساپ  
- زیاد خارجی → آدرس شهر، پیش‌شماره (مثلاً تهران ۰۲۱)، `.ir`، حذف dubai/istanbul  
- اینستاگرام بدون سایت → سایت رسمی / آدرس  
- تکراری زیاد → بوتیک / برند محلی / کوئری جدید  

سپس با باقی‌مانده بودجه دوباره جستجو می‌شود.

### استخراج کاندیدا

یوزرنیم اینستا (نرمال‌شده)، دامنه سایت، تلگرام/واتساپ عمومی، ادغام هویت یک کسب‌وکار. مسیرهای بی‌ربط اینستا (`reel`, `explore`, …) حذف می‌شوند.

---

## ۷. گیت احراز ایران

وقتی کشور = ایران:

| وضعیت | اقدام |
| --- | --- |
| `NOT_IRAN` | رد سخت |
| `UNCERTAIN` | رد |
| `LIKELY_IRAN` | فقط استثنا با ≥۲ منبع مستقل + تلفن یا `.ir` |
| `VERIFIED_IRAN` | ترجیح؛ معمولاً ≥۲ سیگنال مستقل معنادار |

فارسی بودن، یوزرنیم فارسی، فالوور ایرانی، ذکر کلمه ایران، یا آمدن در کوئری ایران **به‌تنهایی کافی نیست**.

چند اسنیپت از یک دامنه = یک منبع.

تعارض مکان (اینستا تهران، سایت دبی) ثبت می‌شود و معمولاً `NOT_IRAN`؛ منبع مطابق درخواست انتخاب نمی‌شود.

`.ir` قوی است ولی باید هویت همان کسب‌وکار باشد. `.com` اگر شواهد مکان ایران باشد مجاز است.

---

## ۸. بقیه احراز و امتیاز

رد کسب‌وکار: پیج شخصی، فن، خبر، سلبریتی، اینفلوئنسر بدون فروش، اسپم، رقیب، شواهد تجاری ناکافی.

فروش آنلاین: اینستاگرامِ خالی فروشگاه حساب نمی‌شود؛ باید کاتالوگ/سفارش/چت‌فروش/سایت فروش باشد.

فعالیت نامشخص اگر تاریخ قابل اتکا نباشد: `UNKNOWN`.

### وزن امتیاز (جمع ۱۰۰)

| بخش | وزن |
| --- | ---: |
| اصالت کسب‌وکار | ۱۵ |
| نیت تجاری | ۱۵ |
| قدرت فروش آنلاین | ۱۵ |
| پتانسیل پیام مشتری | ۱۵ |
| ShopBot Fit | ۱۵ |
| پیچیدگی کاتالوگ | ۱۰ |
| فعالیت | ۵ |
| تماس‌پذیری | ۵ |
| اعتبار/مقیاس | ۵ |

ایران نمره نیست؛ گیت است.

سطح: ۹۰–۱۰۰ HOT، ۷۵–۸۹ HIGH، ۶۰–۷۴ MEDIUM، ۴۰–۵۹ LOW، ۰–۳۹ REJECT.

رتبه بر اساس فالوور نیست: اطمینان احراز → کیفیت ایران → اصالت → نیت تجاری → فروش آنلاین → ShopBot → پیام مشتری → کیفیت شواهد → فعالیت → تماس → نمره کل.

تعداد درخواستی **هدف** است نه تضمین. اگر ۷ لید معتبر باشد، ۷ تا برمی‌گردد.

---

## ۹. دِدوپ و آرشیو

کلید اصلی: یوزرنیم نرمال (`@`، حروف کوچک، URL اینستا، اسلش انتهایی).  
همچنین دامنه سایت و نام کسب‌وکار + شهر.

جلسه جاری + کل `data/leads.json`.

صفحه `/leads` همان آرشیو تاریخی است؛ جستجوی جدید این رکوردها را دوباره به‌عنوان لید جدید نشان نمی‌دهد.

---

## ۱۰. AI

`analyzeLead()` / `scoreLead()`.  
OpenAI-سازگار با `OPENAI_API_KEY` و اختیاری `OPENAI_BASE_URL` (از جمله endpoint شبیه Arena).  
خروجی باید از اسکیمای Zod بگذرد. JSON خراب، فیلد ناقص، یا `inventedClaims` نادیده گرفته می‌شود و پایپلاین ادامه می‌یابد. شهر پیشنهادی AI فقط اگر در متن شواهد باشد استفاده می‌شود.

---

## ۱۱. امنیت و محدودیت پلتفرم

هرگز: لاگین اینستاگرام، ذخیره پسورد/کوکی/سشن، DM، فالو، لایک، کامنت، دور زدن کپچا/ریت‌لیمیت، پروکسی برای فرار، اسکرپ اکانت خصوصی، ساخت اکانت، جعل هویت، outreach خودکار.

سایر:

- اعتبارسنجی ورودی (zod)  
- rate limit حافظه‌ای  
- پیام خطا بدون افشای کلید  
- فایل فقط `data/leads.json`  
- `ENABLE_PAGE_FETCH=false` پیش‌فرض؛ محافظ SSRF در `lib/security/ssrf.ts` اگر بعداً fetch روشن شود  

---

## ۱۲. تست و اجرای لوکال

Vitest: وضعیت‌های ایران، استقلال منبع، مثبت کاذب (فقط فارسی / خارجی فارسی‌زبان / امارات / فقط فالوور)، `.com` و `.ir`، تعارض مکان، رد کسب‌وکار/فروشگاه، رتبه در برابر فالوور، نرمال یوزرنیم/URL، اسکیمای AI، خانواده‌های کوئری، شکست بدون Provider.

آخرین اجرا در توسعه: ۳۱ تست پاس.

```bash
npm install
# .env.local: SEARCH_PROVIDER=mock   یا کلید واقعی
npm run dev          # http://localhost:3500
npm test
npm run typecheck
npm run lint
npm run build
```

اسکریپت‌ها: `dev` و `start` روی پورت **3500**.

برای جستجوی واقعی در `.env.local`:

```
SEARCH_PROVIDER=tavily
TAVILY_API_KEY=...
```

(یا Serper / Brave)

---

## ۱۳. محدودیت‌های باقی‌مانده

- بدون کلید جستجو فقط mock  
- HTML سایت به‌صورت پیش‌فرض fetch نمی‌شود؛ احراز روی عنوان/اسنیپت/URL عمومی است  
- شمار فالوور و تعداد دقیق محصول ساخته نمی‌شود  
- جستجوی وب عمومی ناقص است و ممکن است کسب‌وکار را از دست بدهد یا اشتباه نسبت دهد  
- احراز هویت کاربری / دیتابیس / دیپلوی سرور هنوز در محدوده این نسخه نیست  

---

## ۱۴. کیفیت نهایی مورد انتظار

برای «صنف + ایران + شهر» سیستم باید اولویت بدهد به:

واقعی + فعال + تجاری + مرتبط + ایرانی + احرازشده/پشتیبانی‌شده با شواهد + ShopBot fit قوی + جدید

و رد کند: صفحه فارسی عمومی، صفحه صرفاً مرتبط با ایران، کسب‌وکار خارجی (حتی فروش به ایران)، فرد، اینفلوئنسر، خبر، فن، صفحه تجاری ضعیف، تکراری، حدس بدون شاهد.

سؤال اصلی: آیا می‌توان به `VERIFIED_IRAN` اعتماد کرد چون چند سیگنال عمومی مستقل آن را پشتیبانی می‌کنند؟ اگر نه، آن لید نباید به‌عنوان ایرانی تأییدشده برگردد.
